import 'jsr:@supabase/functions-js/edge-runtime.d.ts';
import { createClient } from 'npm:@supabase/supabase-js@2.95.0';
const sb=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,{auth:{persistSession:false,autoRefreshToken:false}});
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'GET, OPTIONS','Cache-Control':'public, max-age=60','Content-Type':'application/json; charset=utf-8'};
const map=(r:any)=>({date:r.pool_date,no:r.match_no,league:r.league,home:r.home_team,away:r.away_team,kickoff:r.kickoff_at,mode:r.selection_mode,direction:r.public_direction,handicap:r.handicap_direction,confidence:r.confidence==null?null:Math.round(Number(r.confidence)*100),verified:r.result_verified===true,result:r.ft_result,top1Hit:r.ft_top1_hit,coverageHit:r.coverage_hit,handicapHit:r.handicap_hit,version:r.model_version});
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response(null,{headers:cors});try{const u=new URL(req.url);const view=u.searchParams.get('view')||'today';let q=sb.from('soren_publish_cards_v1').select('pool_date,match_no,league,home_team,away_team,kickoff_at,selection_mode,public_direction,handicap_direction,confidence,result_verified,ft_result,ft_top1_hit,coverage_hit,handicap_hit,model_version');if(view==='history'){q=q.eq('result_verified',true).order('pool_date',{ascending:false}).order('match_no',{ascending:true}).limit(120)}else{let date=u.searchParams.get('date');if(!date){const {data:d,error:e}=await sb.from('soren_publish_cards_v1').select('pool_date').order('pool_date',{ascending:false}).limit(1).maybeSingle();if(e)throw e;date=d?.pool_date}q=q.eq('pool_date',date).order('match_no',{ascending:true})}const {data,error}=await q;if(error)throw error;const rows=(data||[]).map(map);return new Response(JSON.stringify({ok:true,view,updatedAt:new Date().toISOString(),rows}),{headers:cors})}catch(e){return new Response(JSON.stringify({ok:false,error:'PUBLIC_API_ERROR'}),{status:500,headers:cors})}});
