name: Lazq prematch capture

on:
  push:
    paths:
      - 'scripts/lazq_capture.py'
      - '.github/workflows/lazq-capture.yml'
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: lazq-prematch-capture
  cancel-in-progress: false

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Capture latest public Lazq data
        run: python scripts/lazq_capture.py
      - name: Commit changed snapshots
        run: |
          if git diff --quiet -- data/lazq; then
            echo "No source change"
            exit 0
          fi
          git config user.name "soren-capture-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/lazq
          git commit -m "data: update Lazq latest snapshot"
          git push
