 void auth(String em,String pw,boolean reg,String name){new Thread(()->{try{JSONObject b=new JSONObject();b.put("email",em);b.put("password",pw);if(reg&&!name.isEmpty()){JSONObject m=new JSONObject();m.put("display_name",name);b.put("data",m);}JSONObject r=postJson(API+(reg?"/auth/v1/signup":"/auth/v1/token?grant_type=password"),b,false);if(reg&&r.optString("access_token").isEmpty()){runOnUiThread(()->Toast.makeText(this,"注册成功，请按邮箱提示确认后登录",Toast.LENGTH_LONG).show());return;}saveSession(r,em);loadAccount();runOnUiThread(()->{Toast.makeText(this,reg?"注册并登录成功":"登录成功",Toast.LENGTH_SHORT).show();render();});}catch(Exception ex){runOnUiThread(()->Toast.makeText(this,"操作失败："+ex.getMessage(),Toast.LENGTH_LONG).show());}}).start();}
 void forgotDialog(){EditText e=new EditText(this);e.setHint("注册邮箱");new AlertDialog.Builder(this).setTitle("找回密码").setView(e).setNegativeButton("取消",null).setPositiveButton("发送邮件",(d,w)->new Thread(()->{try{JSONObject b=new JSONObject();b.put("email",e.getText().toString().trim());postJson(API+"/auth/v1/recover",b,false);runOnUiThread(()->Toast.makeText(this,"重置邮件已发送",Toast.LENGTH_LONG).show());}catch(Exception ex){runOnUiThread(()->Toast.makeText(this,"发送失败",Toast.LENGTH_LONG).show());}}).start()).show();}
 void saveSession(JSONObject r,String em){token=r.optString("access_token");refresh=r.optString("refresh_token");JSONObject u=r.optJSONObject("user");uid=u==null?"":u.optString("id");email=em;sp.edit().putString("token",token).putString("refresh",refresh).putString("uid",uid).putString("email",email).apply();}
 void logout(){token=refresh=uid=email="";profile=new JSONObject();wallet=new JSONObject();orders=new JSONArray();sp.edit().clear().apply();render();}
 void rechargeDialog(){ScrollView sc=new ScrollView(this);LinearLayout x=new LinearLayout(this);x.setOrientation(LinearLayout.VERTICAL);x.setPadding(dp(18),0,dp(18),dp(10));x.addView(tv("支付宝人工充值",21,dark,true));x.addView(tv("先打开支付宝完成付款，再回到这里提交充值申请。只有管理员确认实际到账后才会增加余额或开通会员。",13,muted,false));Button pay=new Button(this);pay.setText("打开支付宝付款");pay.setOnClickListener(v->{try{startActivity(new Intent(Intent.ACTION_VIEW,android.net.Uri.parse("https://qr.alipay.com/tsx19588pb0no8i91mp4cae")));}catch(Exception e){Toast.makeText(this,"无法打开支付宝，请使用收款链接",Toast.LENGTH_LONG).show();}});x.addView(pay);x.addView(tv("收款链接：https://qr.alipay.com/tsx19588pb0no8i91mp4cae",11,muted,false));EditText ref=new EditText(this);ref.setHint("付款备注/支付宝订单尾号（建议填写）");x.addView(ref);int[][] pk={{5000,30},{10000,90},{30000,365}};String[] txt={"¥50 · 30天会员","¥100 · 90天会员","¥300 · 365天会员"};for(int i=0;i<pk.length;i++){int[] q=pk[i];Button b=new Button(this);b.setText(txt[i]);b.setOnClickListener(v->submitRecharge(q[0],q[1],ref.getText().toString().trim()));x.addView(b);}sc.addView(x);new AlertDialog.Builder(this).setTitle("充值中心").setView(sc).setNegativeButton("关闭",null).show();}
