 void sync(boolean toast){new Thread(()->{try{JSONArray f=getArray(API+"/rest/v1/hao_app_full_snapshot_v14?select=publish_date,match_no,league,home_team,away_team,kickoff_bjt,eval_status,result_text&order=publish_date.desc,match_no.asc&limit=500",false);JSONArray p=getArray(API+"/rest/v1/hao_app_picks_snapshot_v14?select=publish_date,match_no,league,home_team,away_team,kickoff_bjt,category,selected_pick,selected_price,recommendation_rank,star_level,short_reason,settle_status,result_text,record_kind&order=publish_date.desc,recommendation_rank.asc&limit=500",false);JSONArray r=getArray(API+"/rest/v1/hao_app_risk_stats_v15?select=publish_date,match_no,risk_badge,risk_reason,risk_status,source_kind&order=publish_date.desc,match_no.asc&limit=500",false);full=f;picks=p;risks=r;if(!token.isEmpty())loadAccount();runOnUiThread(()->{if(day.isEmpty())day=latestDate();render();if(toast)Toast.makeText(this,"已同步最新数据",Toast.LENGTH_SHORT).show();});}catch(Exception e){runOnUiThread(()->{if(toast)Toast.makeText(this,"同步失败："+e.getMessage(),Toast.LENGTH_LONG).show();});}}).start();}
 void loadAccount(){if(token.isEmpty())return;try{JSONArray a=getArray(API+"/rest/v1/hao_app_profiles?select=user_id,display_name,role,account_status,membership_tier,membership_expires_at&user_id=eq."+enc(uid),true);if(a.length()>0)profile=a.optJSONObject(0);JSONArray w=getArray(API+"/rest/v1/hao_app_wallets?select=balance_cents,frozen_cents,currency&user_id=eq."+enc(uid),true);if(w.length()>0)wallet=w.optJSONObject(0);orders=getArray(API+"/rest/v1/hao_app_recharge_orders?select=id,amount_cents,status,channel,created_at,paid_at&user_id=eq."+enc(uid)+"&order=created_at.desc&limit=50",true);}catch(Exception ignored){}}
 JSONArray getArray(String url,boolean auth)throws Exception{HttpURLConnection c=(HttpURLConnection)new URL(url).openConnection();c.setConnectTimeout(12000);c.setReadTimeout(12000);headers(c,auth);int code=c.getResponseCode();String s=read(c,code);if(code>=300)throw new IOException("HTTP "+code+" "+s);return new JSONArray(s);}
 JSONObject postJson(String url,JSONObject b,boolean auth)throws Exception{HttpURLConnection c=(HttpURLConnection)new URL(url).openConnection();c.setRequestMethod("POST");c.setDoOutput(true);c.setConnectTimeout(12000);c.setReadTimeout(20000);headers(c,auth);c.setRequestProperty("Content-Type","application/json");c.getOutputStream().write(b.toString().getBytes(StandardCharsets.UTF_8));int code=c.getResponseCode();String s=read(c,code);if(code>=300)throw new IOException("HTTP "+code+" "+s);return s.trim().startsWith("{")?new JSONObject(s):new JSONObject().put("value",s);}
 JSONArray postArray(String url,JSONObject b,boolean auth)throws Exception{HttpURLConnection c=(HttpURLConnection)new URL(url).openConnection();c.setRequestMethod("POST");c.setDoOutput(true);c.setConnectTimeout(12000);c.setReadTimeout(20000);headers(c,auth);c.setRequestProperty("Content-Type","application/json");c.getOutputStream().write(b.toString().getBytes(StandardCharsets.UTF_8));int code=c.getResponseCode();String s=read(c,code);if(code>=300)throw new IOException("HTTP "+code+" "+s);return new JSONArray(s);}
 void headers(HttpURLConnection c,boolean auth){c.setRequestProperty("apikey",KEY);c.setRequestProperty("Accept","application/json");if(auth&&!token.isEmpty())c.setRequestProperty("Authorization","Bearer "+token);else c.setRequestProperty("Authorization","Bearer "+KEY);}
 String read(HttpURLConnection c,int code)throws Exception{InputStream in=code<300?c.getInputStream():c.getErrorStream();if(in==null)return"";return new String(in.readAllBytes(),StandardCharsets.UTF_8);}
 String latestDate(){String d="";for(int i=0;i<full.length();i++){String x=full.optJSONObject(i).optString("publish_date");if(x.compareTo(d)>0)d=x;}return d;}
 boolean hasPicks(String d,String n){return !pickList(d,n).isEmpty();} ArrayList<JSONObject> pickList(String d,String n){ArrayList<JSONObject>a=new ArrayList<>();for(int i=0;i<picks.length();i++){JSONObject p=picks.optJSONObject(i);if(d.equals(p.optString("publish_date"))&&fmtNo(n).equals(fmtNo(p.optString("match_no"))))a.add(p);}a.sort((x,y)->Integer.compare(x.optInt("recommendation_rank",99),y.optInt("recommendation_rank",99)));return a;}
 JSONObject riskFor(String d,String n){for(int i=0;i<risks.length();i++){JSONObject r=risks.optJSONObject(i);if(d.equals(r.optString("publish_date"))&&fmtNo(n).equals(fmtNo(r.optString("match_no"))))return r;}return new JSONObject();}
 String fmtNo(String n){try{return String.format(Locale.CHINA,"%03d",Integer.parseInt(n));}catch(Exception e){return n;}} String stars(int n){if(n<=0)return"";StringBuilder s=new StringBuilder();for(int i=0;i<Math.min(5,n);i++)s.append("★");return s.toString();}
 String catName(String c){if(c==null)return"精选";String u=c.toUpperCase(Locale.ROOT);if(u.contains("HTFT")||c.contains("半全场"))return"半全场";if(u.contains("HHAD")||c.contains("让球"))return"让球胜平负";if(u.contains("HAD")||u.contains("FT_HAD")||c.contains("胜平负"))return"胜平负";return"精选玩法";}
 String friendlyStatus(String s){if(s==null||s.isEmpty())return"待赛";return s.replace("评测失败","评测未中");} String statusCn(String s){if(s.equals("pending"))return"待审核";if(s.equals("paid"))return"已到账";if(s.equals("failed"))return"审核未通过";if(s.equals("cancelled"))return"已取消";if(s.equals("refunded"))return"已退款";return s;}
 String sanitize(String s){if(s==null)return"";String[] bad={"GPT","Blind","Second Pick Resolver","Quant","HUR","DTR","DLR","P_raw","P_final","Supabase","revision","executor","API-Football","Forebet","Kickoff","Gemini","Claude","Grok","DeepSeek","Kimi","Qwen"};for(String b:bad)s=s.replace(b,"豪竞核心研判系统");return s;}
 String enc(String s){try{return URLEncoder.encode(s,"UTF-8");}catch(Exception e){return s;}}
}
