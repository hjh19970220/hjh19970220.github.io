 void recordPage(){ArrayList<JSONObject> formal=new ArrayList<>();for(int i=0;i<picks.length();i++){JSONObject p=picks.optJSONObject(i);if(!"正式前瞻".equals(p.optString("record_kind")))continue;String s=p.optString("settle_status");if(s.equals("命中")||s.equals("未中")||s.contains("成功")||s.contains("失败"))formal.add(p);}LinearLayout c=card();c.addView(tv("正式实战战绩",22,dark,true));int[] a7=stat(formal,7),a30=stat(formal,30),all=stat(formal,99999);c.addView(tv("近7日  "+fmtStat(a7)+"\n近30日  "+fmtStat(a30)+"\n累计  "+fmtStat(all),15,dark,true));c.addView(tv("只统计真正赛前冻结且赛果确认的正式前瞻；历史回放不计入实战。",12,muted,false));body.addView(c);LinkedHashMap<String,ArrayList<JSONObject>> g=new LinkedHashMap<>();for(JSONObject p:formal){String k=p.optString("publish_date")+"|"+p.optString("match_no");g.computeIfAbsent(k,x->new ArrayList<>()).add(p);}for(ArrayList<JSONObject>a:g.values()){JSONObject f=a.get(0);LinearLayout x=card();x.addView(tv(f.optString("publish_date")+"  "+fmtNo(f.optString("match_no"))+"  "+f.optString("league"),12,muted,true));x.addView(tv(f.optString("home_team")+" vs "+f.optString("away_team"),16,dark,true));x.addView(tv((f.optString("result_text").isEmpty()?"赛果待确认":"比分 "+f.optString("result_text"))+"  "+featuredStatus(a),13,featuredStatus(a).contains("成功")?green:red,true));body.addView(x);}}
 int[] stat(ArrayList<JSONObject>a,int days){if(a.isEmpty())return new int[]{0,0};Date max=null;SimpleDateFormat f=new SimpleDateFormat("yyyy-MM-dd",Locale.CHINA);for(JSONObject p:a)try{Date d=f.parse(p.optString("publish_date"));if(max==null||d.after(max))max=d;}catch(Exception ignored){}HashSet<String> seen=new HashSet<>();int n=0,hits=0;for(JSONObject p:a){try{Date d=f.parse(p.optString("publish_date"));if(max!=null&&(max.getTime()-d.getTime())>days*86400000L)continue;}catch(Exception ignored){}String k=p.optString("publish_date")+"|"+p.optString("match_no");if(seen.contains(k))continue;seen.add(k);ArrayList<JSONObject> same=new ArrayList<>();for(JSONObject q:a)if(k.equals(q.optString("publish_date")+"|"+q.optString("match_no")))same.add(q);n++;if(featuredStatus(same).contains("成功"))hits++;}return new int[]{hits,n};}
 String fmtStat(int[]s){return s[1]==0?"暂无样本":s[0]+"/"+s[1]+"  "+String.format(Locale.CHINA,"%.1f%%",100.0*s[0]/s[1])+"  样本"+s[1];}
 void mePage(){if(token.isEmpty()){LinearLayout c=card();c.addView(tv("登录豪竞",22,dark,true));c.addView(tv("登录后可查看会员状态、余额、充值记录和会员内容。",13,muted,false));Button l=new Button(this);l.setText("登录");l.setOnClickListener(v->loginDialog(false));c.addView(l);Button r=new Button(this);r.setText("注册");r.setOnClickListener(v->loginDialog(true));c.addView(r);Button f=new Button(this);f.setText("忘记密码");f.setOnClickListener(v->forgotDialog());c.addView(f);body.addView(c);return;}LinearLayout p=card();String name=profile.optString("display_name",email);p.addView(tv(name.isEmpty()?"豪竞用户":name,22,dark,true));p.addView(tv("状态："+profile.optString("account_status","正常")+" · 会员："+profile.optString("membership_tier","普通用户"),13,muted,false));String exp=profile.optString("membership_expires_at");if(!exp.isEmpty())p.addView(tv("会员到期："+exp.replace("T"," "),13,muted,false));long bal=wallet.optLong("balance_cents",0);p.addView(tv("账户余额  ¥"+String.format(Locale.CHINA,"%.2f",bal/100.0),24,orange,true));body.addView(p);LinearLayout w=card();w.addView(tv("我的钱包",18,dark,true));Button rc=new Button(this);rc.setText("充值中心");rc.setOnClickListener(v->rechargeDialog());w.addView(rc);Button log=new Button(this);log.setText("充值记录");log.setOnClickListener(v->ordersDialog());w.addView(log);Button rights=new Button(this);rights.setText("会员权益");rights.setOnClickListener(v->new AlertDialog.Builder(this).setTitle("会员权益").setMessage("会员内容将按后台权限开放。充值到账后，会员期限按所选套餐自动更新。\n\n当前支付采用支付宝人工核验，不会伪装成自动支付。").setPositiveButton("知道了",null).show());w.addView(rights);if("admin".equals(profile.optString("role"))){Button ad=new Button(this);ad.setText("充值审核");ad.setOnClickListener(v->adminOrders());w.addView(ad);}Button out=new Button(this);out.setText("退出登录");out.setOnClickListener(v->logout());w.addView(out);body.addView(w);}
 void loginDialog(boolean register){LinearLayout x=new LinearLayout(this);x.setPadding(dp(20),0,dp(20),0);x.setOrientation(LinearLayout.VERTICAL);EditText e=new EditText(this);e.setHint("邮箱");EditText p=new EditText(this);p.setHint("密码（至少6位）");p.setInputType(InputType.TYPE_CLASS_TEXT|InputType.TYPE_TEXT_VARIATION_PASSWORD);EditText n=new EditText(this);n.setHint("昵称");x.addView(e);x.addView(p);if(register)x.addView(n);new AlertDialog.Builder(this).setTitle(register?"注册":"登录").setView(x).setNegativeButton("取消",null).setPositiveButton(register?"注册":"登录",(d,w)->auth(e.getText().toString().trim(),p.getText().toString(),register,n.getText().toString().trim())).show();}
