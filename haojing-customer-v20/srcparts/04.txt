 void recordPage(){ArrayList<JSONObject> formal=new ArrayList<>(),hist=new ArrayList<>();for(int i=0;i<picks.length();i++){JSONObject q=picks.optJSONObject(i);String ss=q.optString("settle_status");if(!(ss.equals("命中")||ss.equals("未中")||ss.contains("成功")||ss.contains("失败")))continue;if("正式前瞻".equals(q.optString("record_kind")))formal.add(q);else if("历史回放".equals(q.optString("record_kind")))hist.add(q);}LinearLayout c=card();c.addView(tv("历史战绩",22,dark,true));int[] h7=stat(hist,7),h30=stat(hist,30),hall=stat(hist,99999);c.addView(tv("历史回放\n近7日  "+fmtStat(h7)+"\n近30日  "+fmtStat(h30)+"\n累计  "+fmtStat(hall),14,dark,true));c.addView(tv("历史回放用于模型复核，不冒充当日公开实战。",11,muted,false));body.addView(c);LinearLayout f=card();f.addView(tv("正式前瞻实战",19,dark,true));int[] f7=stat(formal,7),f30=stat(formal,30),fall=stat(formal,99999);f.addView(tv("近7日  "+fmtStat(f7)+"\n近30日  "+fmtStat(f30)+"\n累计  "+fmtStat(fall),14,dark,true));f.addView(tv("只统计真正赛前冻结且赛果确认的正式发布记录。",11,muted,false));body.addView(f);ArrayList<JSONObject> all=new ArrayList<>();all.addAll(formal);all.addAll(hist);LinkedHashMap<String,ArrayList<JSONObject>> g=new LinkedHashMap<>();for(JSONObject q:all){String k=q.optString("publish_date")+"|"+q.optString("match_no");g.computeIfAbsent(k,x->new ArrayList<>()).add(q);}for(ArrayList<JSONObject>a:g.values()){JSONObject z=a.get(0);LinearLayout x=card();x.addView(tv(z.optString("publish_date")+"  "+fmtNo(z.optString("match_no"))+"  "+z.optString("league"),11,muted,true));x.addView(tv(z.optString("home_team")+" vs "+z.optString("away_team"),15,dark,true));String st=featuredStatus(a);x.addView(tv((z.optString("result_text").isEmpty()?"赛果待确认":"比分 "+z.optString("result_text"))+"   "+st,12,st.contains("成功")?green:red,true));body.addView(x);}}
 int[] stat(ArrayList<JSONObject>a,int days){if(a.isEmpty())return new int[]{0,0};Date max=null;SimpleDateFormat f=new SimpleDateFormat("yyyy-MM-dd",Locale.CHINA);for(JSONObject p:a)try{Date d=f.parse(p.optString("publish_date"));if(max==null||d.after(max))max=d;}catch(Exception ignored){}HashSet<String> seen=new HashSet<>();int n=0,hits=0;for(JSONObject p:a){try{Date d=f.parse(p.optString("publish_date"));if(max!=null&&(max.getTime()-d.getTime())>days*86400000L)continue;}catch(Exception ignored){}String k=p.optString("publish_date")+"|"+p.optString("match_no");if(seen.contains(k))continue;seen.add(k);ArrayList<JSONObject> same=new ArrayList<>();for(JSONObject q:a)if(k.equals(q.optString("publish_date")+"|"+q.optString("match_no")))same.add(q);n++;if(featuredStatus(same).contains("成功"))hits++;}return new int[]{hits,n};}
 String fmtStat(int[]s){return s[1]==0?"暂无样本":s[0]+"/"+s[1]+"  "+String.format(Locale.CHINA,"%.1f%%",100.0*s[0]/s[1])+"  样本"+s[1];}
 void depthPage(){LinearLayout h=card();h.addView(tv("深度研判",22,dark,true));h.addView(tv("客户只看结论，不展示底层算法、模型名称和内部字段。",12,muted,false));body.addView(h);String[][] items={{"核心研判","综合实力、赛前信息与市场方向形成客户结论"},{"平局识别","识别比赛胶着度以及平局尾部风险"},{"冷门预警","热门存在明显隐患时主动提示回避"},{"让球判断","区分能否赢球与净胜空间"},{"风险审计","多项信号冲突时降级或直接 PASS"},{"使用说明","全池负责稳定覆盖，精选只留真正高质量场次"}};for(String[] it:items){final String title=it[0],sub=it[1];LinearLayout c=card();c.addView(tv(title,17,dark,true));c.addView(tv(sub,12,muted,false));TextView arrow=tv("查看详情  ›",12,orange,true);arrow.setGravity(Gravity.RIGHT);c.addView(arrow);c.setClickable(true);c.setOnClickListener(v->depthDetail(title));body.addView(c);}}
 void depthDetail(String title){String msg="";if(title.equals("核心研判"))msg="豪竞核心研判系统会综合球队实力、赛前状态、市场方向和关键情报，最终只输出客户需要看到的结论。\n\n当多个方向冲突时，不会为了凑推荐强行给答案。";else if(title.equals("平局识别"))msg="用于识别比赛是否存在明显胶着结构。\n\n当平局风险较高时，系统会提高平局方向权重，或降低热门方向信心；风险过高时直接 PASS。";else if(title.equals("冷门预警"))msg="当热门方存在明显失手风险、市场支持不足或赛前信息出现冲突时，会显示冷门预警。\n\n预警不等于一定爆冷，而是提醒客户降低投注强度或回避。";else if(title.equals("让球判断"))msg="普通胜负与让球结果不是同一件事。\n\n系统会分别判断球队能否取胜，以及可能的净胜空间，再决定是否进入让球精选。";else if(title.equals("风险审计"))msg="每场正式精选发布前都会进行风险复核。\n\n如果核心信息冲突、数据质量不足或推荐优势不明显，比赛会被降级，必要时直接 PASS。";else msg="首页负责浏览全部比赛；精选页只展示达到门槛的少量比赛；预警页用于查看高风险场次。\n\n精选不追求每天数量，宁可少推，也不为了凑场次降低标准。";new AlertDialog.Builder(this).setTitle(title).setMessage(msg).setPositiveButton("知道了",null).show();}
 void mePage(){if(token.isEmpty()){LinearLayout c=card();c.setBackground(box(0xFFFFF4E6,18));c.addView(tv("登录豪竞2.7",23,dark,true));c.addView(tv("登录后查看会员内容、余额、充值记录与个人权益。",13,muted,false));Button l=new Button(this);l.setText("登录");l.setOnClickListener(v->loginDialog(false));c.addView(l);Button r=new Button(this);r.setText("创建账号");r.setOnClickListener(v->loginDialog(true));c.addView(r);Button f=new Button(this);f.setText("忘记密码");f.setOnClickListener(v->forgotDialog());c.addView(f);body.addView(c);return;}LinearLayout p=card();p.setBackground(box(dark,18));String name=profile.optString("display_name",email);p.addView(tv(name.isEmpty()?"豪竞用户":name,22,Color.WHITE,true));p.addView(tv("会员："+profile.optString("membership_tier","普通用户")+"  ·  状态："+profile.optString("account_status","正常"),12,0xFFD6D6D6,false));String exp=profile.optString("membership_expires_at");if(!exp.isEmpty())p.addView(tv("有效期至："+exp.replace("T"," "),12,0xFFD6D6D6,false));long bal=wallet.optLong("balance_cents",0);p.addView(tv("¥ "+String.format(Locale.CHINA,"%.2f",bal/100.0),25,0xFFFFB23F,true));p.addView(tv("账户余额",11,0xFFD6D6D6,false));body.addView(p);LinearLayout w=card();w.addView(tv("个人中心",18,dark,true));Button member=new Button(this);member.setText("我的会员");member.setOnClickListener(v->new AlertDialog.Builder(this).setTitle("会员权益").setMessage("会员内容按后台权限开放；正式前瞻、精选详情和后续高级功能将按会员等级开放。").setPositiveButton("知道了",null).show());w.addView(member);Button rc=new Button(this);rc.setText("充值中心");rc.setOnClickListener(v->rechargeDialog());w.addView(rc);Button log=new Button(this);log.setText("充值记录");log.setOnClickListener(v->ordersDialog());w.addView(log);if("admin".equals(profile.optString("role"))){Button ad=new Button(this);ad.setText("充值审核");ad.setOnClickListener(v->adminOrders());w.addView(ad);}Button out=new Button(this);out.setText("退出登录");out.setOnClickListener(v->logout());w.addView(out);body.addView(w);}
 void loginDialog(boolean register){LinearLayout x=new LinearLayout(this);x.setPadding(dp(20),0,dp(20),0);x.setOrientation(LinearLayout.VERTICAL);EditText e=new EditText(this);e.setHint("邮箱");EditText p=new EditText(this);p.setHint("密码（至少6位）");p.setInputType(InputType.TYPE_CLASS_TEXT|InputType.TYPE_TEXT_VARIATION_PASSWORD);EditText n=new EditText(this);n.setHint("昵称");x.addView(e);x.addView(p);if(register)x.addView(n);new AlertDialog.Builder(this).setTitle(register?"创建账号":"登录豪竞").setView(x).setNegativeButton("取消",null).setPositiveButton(register?"注册":"登录",(d,w)->auth(e.getText().toString().trim(),p.getText().toString(),register,n.getText().toString().trim())).show();}
