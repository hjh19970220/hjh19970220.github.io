 LinearLayout homeCard(JSONObject o){LinearLayout c=card();String d=o.optString("publish_date"),no=fmtNo(o.optString("match_no"));ArrayList<JSONObject> ps=pickList(d,o.optString("match_no"));LinearLayout top=new LinearLayout(this);top.setGravity(Gravity.CENTER_VERTICAL);top.addView(tv(no+"  "+o.optString("league"),12,muted,true),new LinearLayout.LayoutParams(0,-2,1));if(!ps.isEmpty()){TextView tag=tv("精选",11,Color.WHITE,true);tag.setBackground(box(orange,10));tag.setPadding(dp(8),dp(3),dp(8),dp(3));top.addView(tag);}c.addView(top);TextView teams=tv(o.optString("home_team")+"   VS   "+o.optString("away_team"),17,dark,true);teams.setGravity(Gravity.CENTER);teams.setPadding(0,dp(10),0,dp(8));c.addView(teams);String rs=o.optString("result_text");String st=ps.isEmpty()?"":featuredStatus(ps);LinearLayout foot=new LinearLayout(this);foot.setGravity(Gravity.CENTER_VERTICAL);foot.addView(tv(rs.isEmpty()?"未开赛":"完场  "+rs,12,muted,false),new LinearLayout.LayoutParams(0,-2,1));if(!st.isEmpty())foot.addView(tv(st,12,st.contains("成功")?green:st.contains("未中")?red:orange,true));c.addView(foot);c.setOnClickListener(v->detail(o));return c;}
 void detail(JSONObject o){String d=o.optString("publish_date"),n=o.optString("match_no");ArrayList<JSONObject> ps=pickList(d,n);LinearLayout wrap=new LinearLayout(this);wrap.setOrientation(LinearLayout.VERTICAL);wrap.setPadding(dp(18),0,dp(18),0);wrap.addView(tv(fmtNo(n)+"  "+o.optString("league"),12,orange,true));TextView tm=tv(o.optString("home_team")+"  VS  "+o.optString("away_team"),21,dark,true);tm.setGravity(Gravity.CENTER);tm.setPadding(0,dp(8),0,dp(10));wrap.addView(tm);String k=o.optString("kickoff_bjt");if(!k.isEmpty())wrap.addView(tv("比赛时间  "+k.replace("T"," ").replace("+00:00",""),12,muted,false));wrap.addView(tv("核心推荐",17,dark,true));if(ps.isEmpty())wrap.addView(tv("本场未达到精选标准 · 建议回避",15,muted,true));else{int max=Math.min(2,ps.size());for(int i=0;i<max;i++){JSONObject q=ps.get(i);LinearLayout b=card();b.setBackground(box(i==0?0xFFFFF0E8:0xFFFFF8EE,14));b.addView(tv(catName(q.optString("category")),12,muted,true));b.addView(tv(q.optString("selected_pick"),19,i==0?0xFFD9362B:orange,true));b.addView(tv("信心  "+stars(q.optInt("star_level",0)),12,muted,false));wrap.addView(b);}JSONObject q=ps.get(0);String reason=q.optString("short_reason");if(!reason.isEmpty()){wrap.addView(tv("赛前研判",15,dark,true));wrap.addView(tv(sanitize(reason),13,dark,false));}}JSONObject rr=riskFor(d,n);String reason=rr.optString("risk_reason");if(!reason.isEmpty()){wrap.addView(tv("风险提示",15,red,true));wrap.addView(tv(sanitize(reason),13,0xFFB46A16,false));}String rs=o.optString("result_text");String ev=ps.isEmpty()?"":featuredStatus(ps);if(!rs.isEmpty()){wrap.addView(tv("赛后结果",15,dark,true));wrap.addView(tv("比分  "+rs+(ev.isEmpty()?"":"   "+ev),14,ev.contains("成功")?green:ev.contains("未中")?red:dark,true));}new AlertDialog.Builder(this).setTitle("比赛详情").setView(wrap).setPositiveButton("关闭",null).show();}
 void fullPage(){home();}
 void picksPage(){LinearLayout head=card();head.addView(tv("精选推荐",22,dark,true));head.addView(tv("只展示达到门槛的少量比赛 · 不够强就 PASS",12,muted,false));body.addView(head);LinkedHashMap<String,ArrayList<JSONObject>> groups=new LinkedHashMap<>();for(int i=0;i<picks.length();i++){JSONObject o=picks.optJSONObject(i);if(!day.equals(o.optString("publish_date")))continue;String k=o.optString("match_no");groups.computeIfAbsent(k,x->new ArrayList<>()).add(o);}if(groups.isEmpty()){LinearLayout c=card();c.addView(tv("今日暂无精选",18,dark,true));c.addView(tv("没有达到客户精选门槛的比赛，宁可不推。",13,muted,false));body.addView(c);return;}for(Map.Entry<String,ArrayList<JSONObject>> e:groups.entrySet()){ArrayList<JSONObject> a=e.getValue();a.sort((x,y)->Integer.compare(x.optInt("recommendation_rank",99),y.optInt("recommendation_rank",99)));JSONObject f=a.get(0);LinearLayout c=card();LinearLayout top=new LinearLayout(this);top.addView(tv(fmtNo(e.getKey())+"  "+f.optString("league"),12,muted,true),new LinearLayout.LayoutParams(0,-2,1));String rk=f.optString("record_kind");top.addView(tv(rk.equals("历史回放")?"历史回放":"正式前瞻",11,rk.equals("历史回放")?muted:orange,true));c.addView(top);c.addView(tv(f.optString("home_team")+"  VS  "+f.optString("away_team"),17,dark,true));int max=Math.min(2,a.size());for(int j=0;j<max;j++){JSONObject q=a.get(j);c.addView(tv(catName(q.optString("category"))+"  "+q.optString("selected_pick")+"  "+stars(q.optInt("star_level",0)),15,j==0?0xFFD9362B:orange,true));}String st=featuredStatus(a);c.addView(tv(st,13,st.contains("成功")?green:st.contains("未中")?red:muted,true));c.setOnClickListener(v->{JSONObject faux=new JSONObject();try{faux.put("publish_date",f.optString("publish_date"));faux.put("match_no",f.optString("match_no"));faux.put("league",f.optString("league"));faux.put("home_team",f.optString("home_team"));faux.put("away_team",f.optString("away_team"));faux.put("kickoff_bjt",f.optString("kickoff_bjt"));faux.put("result_text",f.optString("result_text"));}catch(Exception ignored){}detail(faux);});body.addView(c);}}
 String featuredStatus(ArrayList<JSONObject>a){boolean settled=false,hit=false;int max=Math.min(2,a.size());for(int i=0;i<max;i++){JSONObject p=a.get(i);String s=p.optString("settle_status");if(s.equals("命中")||s.contains("成功")){settled=true;hit=true;}else if(s.equals("未中")||s.contains("失败"))settled=true;}return !settled?"待赛":hit?"✅ 评测成功":"❌ 评测未中";}
