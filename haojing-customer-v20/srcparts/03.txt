 LinearLayout homeCard(JSONObject o){LinearLayout c=card();String d=o.optString("publish_date"),no=fmtNo(o.optString("match_no"));ArrayList<JSONObject> ps=pickList(d,o.optString("match_no"));LinearLayout top=new LinearLayout(this);top.addView(tv(no+"  "+o.optString("league"),13,muted,true),new LinearLayout.LayoutParams(0,-2,1));if(!ps.isEmpty())top.addView(tv("精选",12,orange,true));c.addView(top);c.addView(tv(o.optString("home_team")+"  vs  "+o.optString("away_team"),17,dark,true));String rs=o.optString("result_text");String st=ps.isEmpty()?"":featuredStatus(ps);String line=rs.isEmpty()?"待赛":("完场 "+rs+(st.isEmpty()?"":"  "+st));c.addView(tv(line,13,rs.isEmpty()?blue:(st.contains("成功")?green:st.contains("未中")?red:muted),true));c.setOnClickListener(v->detail(o));return c;}
 void detail(JSONObject o){String d=o.optString("publish_date"),n=o.optString("match_no");ArrayList<JSONObject> ps=pickList(d,n);LinearLayout wrap=new LinearLayout(this);wrap.setOrientation(LinearLayout.VERTICAL);wrap.setPadding(dp(18),0,dp(18),0);wrap.addView(tv(fmtNo(n)+"  "+o.optString("league"),13,muted,true));wrap.addView(tv(o.optString("home_team")+"  vs  "+o.optString("away_team"),20,dark,true));String k=o.optString("kickoff_bjt");if(!k.isEmpty())wrap.addView(tv("比赛时间："+k.replace("T"," ").replace("+00:00",""),13,muted,false));if(ps.isEmpty())wrap.addView(tv("本场暂无客户精选方案",15,muted,false));else{wrap.addView(tv("本场两项精选方案",16,dark,true));int max=Math.min(2,ps.size());for(int i=0;i<max;i++){JSONObject p=ps.get(i);String conf=stars(p.optInt("star_level",0));String spx=p.optString("selected_price");wrap.addView(tv(catName(p.optString("category"))+"："+p.optString("selected_pick")+(spx.isEmpty()?"":"  SP "+spx)+"  "+conf,16,orange,true));}JSONObject p=ps.get(0);String reason=p.optString("short_reason");if(!reason.isEmpty())wrap.addView(tv("赛前研判："+sanitize(reason),13,dark,false));}JSONObject rr=riskFor(d,n);String reason=rr.optString("risk_reason");if(!reason.isEmpty())wrap.addView(tv("风险提醒："+sanitize(reason),13,0xFFB46A16,false));String rs=o.optString("result_text");String ev=ps.isEmpty()?"":featuredStatus(ps);if(!rs.isEmpty())wrap.addView(tv("赛后比分："+rs+(ev.isEmpty()?"":"\n评测结果："+ev.replace("✅ ","").replace("❌ ","")),14,dark,true));new AlertDialog.Builder(this).setTitle("比赛详情").setView(wrap).setPositiveButton("关闭",null).show();}
 void fullPage(){LinearLayout c=card();c.addView(tv("全池赛事",21,dark,true));c.addView(tv("客户页只展示比赛状态与精选评测，不强行套“主队不败 / 客队不败”。",13,muted,false));body.addView(c);for(int i=0;i<full.length();i++){JSONObject o=full.optJSONObject(i);if(day.equals(o.optString("publish_date")))body.addView(homeCard(o));}}
 void picksPage(){LinkedHashMap<String,ArrayList<JSONObject>> groups=new LinkedHashMap<>();for(int i=0;i<picks.length();i++){JSONObject o=picks.optJSONObject(i);if(!day.equals(o.optString("publish_date")))continue;String k=o.optString("match_no");groups.computeIfAbsent(k,x->new ArrayList<>()).add(o);}if(groups.isEmpty()){LinearLayout c=card();c.addView(tv("该日期暂无正式客户精选",18,dark,true));c.addView(tv("只有赛前冻结后的方案才会展示；不会赛后补方向。",13,muted,false));body.addView(c);return;}for(Map.Entry<String,ArrayList<JSONObject>> e:groups.entrySet()){ArrayList<JSONObject> a=e.getValue();a.sort((x,y)->Integer.compare(x.optInt("recommendation_rank",99),y.optInt("recommendation_rank",99)));JSONObject f=a.get(0);LinearLayout c=card();c.addView(tv(fmtNo(e.getKey())+"  "+f.optString("league"),13,muted,true));c.addView(tv(f.optString("home_team")+"  vs  "+f.optString("away_team"),17,dark,true));int max=Math.min(2,a.size());for(int j=0;j<max;j++){JSONObject p=a.get(j);String spx=p.optString("selected_price");c.addView(tv(catName(p.optString("category"))+"："+p.optString("selected_pick")+(spx.isEmpty()?"":"  SP "+spx)+"  "+stars(p.optInt("star_level",0)),15,orange,true));}String st=featuredStatus(a);c.addView(tv(st,13,st.contains("成功")?green:st.contains("未中")?red:muted,true));c.setOnClickListener(v->{JSONObject faux=new JSONObject();try{faux.put("publish_date",f.optString("publish_date"));faux.put("match_no",f.optString("match_no"));faux.put("league",f.optString("league"));faux.put("home_team",f.optString("home_team"));faux.put("away_team",f.optString("away_team"));faux.put("kickoff_bjt",f.optString("kickoff_bjt"));faux.put("result_text",f.optString("result_text"));faux.put("eval_status",st);}catch(Exception ignored){}detail(faux);});body.addView(c);}}
 String featuredStatus(ArrayList<JSONObject>a){boolean settled=false,hit=false;int max=Math.min(2,a.size());for(int i=0;i<max;i++){JSONObject p=a.get(i);String s=p.optString("settle_status");if(s.equals("命中")||s.contains("成功")){settled=true;hit=true;}else if(s.equals("未中")||s.contains("失败"))settled=true;}return !settled?"待赛":hit?"✅ 评测成功":"❌ 评测未中";}
